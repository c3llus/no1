package main

const (
	baseDirectoryFormat = `%s/shift2/drakor`
	basePath            = `.`
	zipFileName         = `drakor.zip`
	extPNG              = `.png`
	separator           = `;`
	separatorByte       = ';'
)
